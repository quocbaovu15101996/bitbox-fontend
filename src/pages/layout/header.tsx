import React, { FC } from 'react';

const HeaderLandingComponent: FC = () => {
  return (
    <div id="header" className={''}>
      Header
    </div>
  );
};

export default React.memo(HeaderLandingComponent);
